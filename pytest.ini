[pytest]
# Only consider files named test_*.py
python_files = test_*.py

# Quiet mode, hide warnings, ignore helper scripts & Magenta folder
addopts = -q --disable-warnings --ignore=scripts --ignore=magenta

